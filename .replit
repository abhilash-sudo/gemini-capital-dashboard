# This tells Replit to run both servers at once.
run = "pip install -r requirements.txt && npm install && node server.js & python app.py"

# This tells Replit which files/folders to show in the file tree.
hidden = ["venv", ".config", "package-lock.json"]
